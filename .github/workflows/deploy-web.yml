name: Deploy web to Cloudflare

# Auto-deploy the Next.js app in web/ to Cloudflare Workers on every push to
# master that touches web/. Requires a repo secret CLOUDFLARE_API_TOKEN
# (permission: Workers Scripts → Edit). The account id is pinned in
# web/wrangler.jsonc so the deploy is non-interactive.
on:
  push:
    branches: [master]
    paths:
      - "web/**"
      - ".github/workflows/deploy-web.yml"
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build & deploy (OpenNext → Cloudflare)
        run: npm run cf:deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
